import React from 'react';

const NotFound = () => {
    return (
        <h1 className="text-center">
            Does unicorns exist? No, right?<br />
            So doesn't this page. <br />
            Find a unicorn and this page will also exist.
        </h1>
    );
}

export default NotFound;